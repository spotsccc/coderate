import { Store } from 'effector';
import { StoreMeta, EventMeta, LogMeta, EffectMeta, FilesMap, Options } from './types.h';
export declare function Root($stores: Store<Record<string, StoreMeta>>, $events: Store<Record<string, EventMeta>>, $effects: Store<Record<string, EffectMeta>>, $logs: Store<LogMeta[]>, $files: Store<FilesMap>, options?: Options): void;
