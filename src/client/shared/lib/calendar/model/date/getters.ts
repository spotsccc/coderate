import { Month } from './types'

export const getMonthDays = (month: Month) => month.days
export const getMonthName = (month: Month) => month.name
export const getMonthYear = (month: Month) => month.year
